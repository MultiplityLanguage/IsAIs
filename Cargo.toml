[package]
name = "isais"
version = "0.1.0"
edition = "2024"
description = "A language for LLMs, by LLMs"
authors = ["IsAIs Team"]

[dependencies]
# 暂时不使用外部依赖，使用标准库实现
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
rand = "0.8"